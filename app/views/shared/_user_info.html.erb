<div class="sidebar-footer">
  <div class="d-flex align-items-center gap-3 mb-3">
    <div class="user-avatar">
      <%= current_user.name.first.upcase %>
    </div>
    <div class="flex-grow-1 overflow-hidden">
      <p class="mb-0 fw-medium text-truncate"><%= current_user.name %></p>
      <small class="text-muted text-truncate d-block"><%= current_user.email %></small>
    </div>
  </div>
  <%= link_to destroy_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm w-100" do %>
    <i class="bi bi-box-arrow-left me-2"></i> Cerrar Sesión
  <% end %>
</div>